<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs')%>
    <h1>COURSE CATALOG</h1>
    <h2>Part-time or full-time. Bootcamp. Short course. Workshop. Choose your own tech adventure.</h2>
    <% courses.forEach((course)=>{%>
        <% if(course.active===true){%>
            <h3><a href="/users/<%=user._id%>/courses/<%=course._id%>"><%=course.name%></a></h3>
            <li><%=course.description%></li>
            <li>📍<%=course.studyType%></li>
            <li>🕗<%=course.durations%> weeks</li>
        <%}%>
    <%})%>

    <a href="/users/<%=user._id%>/courses/new">Add New Course</a>
    <a href="/users/<%=user._id%>/courses/reactivateCourse">Reactivate an inactive course</a>
</body>
</html>