<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <!-- < %- include('../partials/_navbar.ejs') % > -->
    <h1><%=course.name%></h1>
    <h2><%=course.description%></h2>
    <li>Topic: <%=course.topic%></li>
    <li>Catalog:  <%=course.catalog%></li>
    <li>📍<%=course.studyType%></li>
    <li>🕗<%=course.duration%> weeks</li>

    <form action="/courses/<%=course._id%>?_method=DELETE" method="POST">
        <button>Delete <%=course.name%></button>
    </form>
    <a href="/courses/<%=course._id%>/edit">Update <%=course.name%></a>
</body>
</html>